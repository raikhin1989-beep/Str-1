# Зависимости приложения. Ставятся на сервере в /opt/str-1/venv.
# Всё, что нужно только для тестов, живёт в requirements-dev.txt и на сервер
# не попадает.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
jinja2>=3.1,<4.0
# Разбор форм админки: без него FastAPI не примет Form(...).
python-multipart>=0.0.9,<1.0
