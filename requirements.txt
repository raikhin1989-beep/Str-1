# Зависимости приложения. Ставятся на сервере в /opt/str-1/venv.
# Всё, что нужно только для тестов, живёт в requirements-dev.txt и на сервер
# не попадает.
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
jinja2>=3.1,<4.0
# Разбор форм админки и загрузка фото: без него FastAPI не примет Form(...).
python-multipart>=0.0.9,<1.0
# Распознавание виски. anthropic — для хостинга вне РФ, httpx — для Яндекса,
# который работает с этого сервера. Кто отвечает, решает app/config.py.
anthropic>=0.116,<1.0
httpx>=0.27,<1.0
# QR-коды для ссылок регистрации. SVG, поэтому Pillow не нужен.
qrcode>=7.4,<9.0
