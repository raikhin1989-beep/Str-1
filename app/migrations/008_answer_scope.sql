-- Из чего участник выбирает ответ.
--
-- Изначально список был закрытым: ровно те виски, что налиты. Это делало
-- конкурс перестановкой, и частичный балл за «тот же класс» почти не работал —
-- промахнуться в свой класс можно было только поменяв местами два образца.
--
-- Теперь по умолчанию выбор из всего справочника: можно назвать виски, которого
-- на столе нет, и если класс совпал — получить балл за класс. Задача из
-- «разложить пять названий» становится «узнать, что это».
--
-- 'tasting' оставлен как режим полегче: список только из налитого. Выбирает
-- админ на странице дегустации, до начала раундов.
ALTER TABLE tasting ADD COLUMN answer_scope TEXT NOT NULL DEFAULT 'catalogue';
